package com.sokolov.javafx.controller.interfaces;

import com.sokolov.javafx.controller.objects.Dok;

public interface TableDok {

    void create(Dok dok);

    void add(Dok dok);

    void update(Dok dok);

    void delete(Dok dok);
}
